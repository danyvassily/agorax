"use client";
import { useRef, useState } from 'react';
import Link from 'next/link';
import { Camera, Check, Settings, LoaderCircle, X } from 'lucide-react';
import { AppNavigation } from '@/components/ui/app-navigation';
import { PlayerDot } from '@/components/ui/primitives';
import { KawaiiMascot } from '@/components/ui/kawaii-mascot';
import { useAuth, compressProfilePhoto, type AuthUser } from '@/lib/auth/use-auth';
import { useLanguageStore } from '@/lib/store/language';
import { type UILanguage } from '@/lib/i18n';
import { CHARACTERS, characterImage } from '@/lib/characters';
export function ProfileClient({character}:{character?:string}){const {user,loading}=useAuth();return <><AppNavigation/><main className="jx-page jx-profile-page">{loading||!user?<div role="status" className="py-20 text-center"><LoaderCircle className="mx-auto animate-spin"/><p>JOUXTA</p></div>:<ProfileEditor key={`${user.id}-${character??''}`} user={user} character={character}/>}</main></>}
function ProfileEditor({user,character}:{user:AuthUser;character?:string}){
 const {updateProfile,isLoggedIn}=useAuth();const currentLanguage=useLanguageStore(s=>s.language);const en=currentLanguage==='en';
 const selectedCharacter=CHARACTERS.find(c=>c.id===character);
 const [name,setName]=useState(user.name);const [language,setLanguage]=useState<UILanguage>(currentLanguage);const [avatar,setAvatar]=useState<string|null>(selectedCharacter?characterImage(selectedCharacter.id):user.avatarUrl??null);const [preview,setPreview]=useState<string|null>(null);const [busy,setBusy]=useState(false);const [reading,setReading]=useState(false);const [saved,setSaved]=useState(false);const [error,setError]=useState('');const fileInput=useRef<HTMLInputElement>(null);
 const dirty=()=>setSaved(false);
 async function photo(file?:File){if(!file)return;setError('');setReading(true);try{setPreview(await compressProfilePhoto(file));}catch{setError(en?'Choose a JPG, PNG or WebP image smaller than 8 MB.':'Choisis une image JPG, PNG ou WebP de moins de 8 Mo.')}finally{setReading(false);if(fileInput.current)fileInput.current.value=''}}
 async function save(){if(!name.trim()||busy)return;setBusy(true);setError('');setSaved(false);try{await updateProfile({name:name.trim(),avatarUrl:avatar,language});setSaved(true)}catch{setError(en?'Your changes could not be saved. Please try again.':'Les modifications n’ont pas pu être enregistrées. Réessaie.')}finally{setBusy(false)}}
 return <><header className="jx-page-title"><span className="jx-eyebrow">{en?'YOUR CORNER OF JOUXTA':'TON COIN À TOI'}</span><h1>{en?'My profile':'Mon profil'}</h1></header><div className="jx-profile-grid"><section className="jx-profile-identity"><PlayerDot name={name} avatarUrl={avatar??characterImage('milo')} size={138}/><h2>{name}</h2><p>{isLoggedIn?(en?'Your JOUXTA account':'Ton compte JOUXTA'):(en?'Guest · Saved on this device':'Invité · Enregistré sur cet appareil')}</p><button disabled={reading||busy} className="fp-btn-secondary" onClick={()=>fileInput.current?.click()}><Camera size={18}/>{reading?(en?'Reading image…':'Lecture de l’image…'):(en?'Add a photo':'Ajouter une photo')}</button><input ref={fileInput} type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>void photo(e.target.files?.[0])} className="sr-only" aria-label={en?'Profile photo':'Photo de profil'}/>{avatar&&<button className="fp-btn-ghost" disabled={busy} onClick={()=>{setAvatar(null);dirty()}}>{en?'Remove photo':'Supprimer la photo'}</button>}<h3>{en?'Or choose your teammate':'Ou choisis ton personnage'}</h3><div className="jx-character-picker">{CHARACTERS.map(c=><button key={c.id} aria-label={c.name} aria-pressed={avatar===characterImage(c.id)} disabled={busy} onClick={()=>{setAvatar(characterImage(c.id));dirty()}} style={{background:c.color}}><KawaiiMascot theme={c.id} size={62}/><span>{c.name}</span></button>)}</div>{isLoggedIn&&user.eloGamesPlayed!==undefined&&<div className="jx-profile-stats"><span><strong>{user.eloGamesPlayed}</strong>{en?'ranked games':'parties classées'}</span><span><strong>{user.eloRating??1000}</strong>ELO</span></div>}</section><section className="jx-profile-form"><label>{en?'Nickname':'Pseudo'}<input className="fp-input" value={name} maxLength={24} disabled={busy} onChange={e=>{setName(e.target.value);dirty()}}/></label><label>{en?'Application language':'Langue de l’application'}<select className="fp-input" value={language} disabled={busy} onChange={e=>{setLanguage(e.target.value as UILanguage);dirty()}}><option value="fr">Français</option><option value="en">English</option></select><small>{en?'The interface and questions, in your language.':'L’interface et les questions dans ta langue.'}</small></label><p className="jx-profile-note">{en?'Your character is your signature. Everyone plays by the same rules.':'Ton personnage, c’est ta signature. Les règles restent les mêmes pour tout le monde.'}</p>{error&&<p className="jx-error" role="alert">{error}</p>}{saved&&<p className="jx-success" role="status"><Check size={18}/>{en?'Profile saved':'Profil enregistré'}</p>}<button className="fp-btn-primary" disabled={busy||reading||!name.trim()} onClick={()=>void save()}>{busy?<LoaderCircle size={18} className="animate-spin"/>:saved?<Check size={18}/>:null}{en?'Save':'Enregistrer'}</button><Link href="/settings" className="fp-btn-secondary"><Settings size={18}/>{en?'Sound and game settings':'Son et réglages des jeux'}</Link><Link href="/auth" className="fp-btn-ghost">{isLoggedIn?(en?'Manage my account':'Gérer mon compte'):(en?'Sign in or create an account':'Se connecter ou créer un compte')}</Link></section></div>{preview&&<div className="jx-modal" role="dialog" aria-modal="true" aria-label={en?'Photo preview':'Aperçu de la photo'}><div><button className="jx-modal-close" aria-label={en?'Cancel':'Annuler'} onClick={()=>setPreview(null)}><X/></button><h2>{en?'Your new photo':'Ta nouvelle photo'}</h2><PlayerDot name={name} avatarUrl={preview} size={190}/><p>{en?'The image is cropped to a square and displayed in a circle.':'L’image est centrée et recadrée pour ton avatar.'}</p><button autoFocus className="fp-btn-primary" onClick={()=>{setAvatar(preview);setPreview(null);dirty()}}>{en?'Use this photo':'Utiliser cette photo'}</button><button className="fp-btn-ghost" onClick={()=>setPreview(null)}>{en?'Cancel':'Annuler'}</button></div></div>}</>;
}
